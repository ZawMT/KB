<?php
// A file may return a value. This is the standard way to store settings -
// nothing leaks into the caller's scope, you just get the array back.

return [
    "host"  => "localhost",
    "port"  => 8080,
    "debug" => true,
];
