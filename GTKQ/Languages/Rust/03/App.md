# App requirement
CLI to-do list (add/list/complete tasks, saved to a JSON file) — uses serde/serde_json for (de)serialization + file I/O. Good for: multi-module project structure (src/main.rs + a couple of .rs files), not just one file.
