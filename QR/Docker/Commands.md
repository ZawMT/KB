#### [Back to Docker contents](_Contents.md)

| Command | Note |
|-----------|------|
| `docker compose down -v` | `-v` tells Docker to also remove the named volumes defined in the compose file when bringing the containers down. for `Named volume`, refer to Docker.md in the KeyPoints folder. |